<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

<% if @user.microposts.first.content %>
  <p>
    <strong>First micropost</strong>
    <br>
    <%= @user.microposts.first.content %>
  </p>
<% end %>